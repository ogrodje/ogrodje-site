---
import EpisodeRow from "./EpisodeRow.astro";
import {EpisodeService} from "../services/EpisodeService";
const episodes = await EpisodeService.lastEpisodes(3);
---
<div class="episodes">
  {episodes.map(episode =>
    (
      <EpisodeRow episode={episode}/>)
  )}
</div>
